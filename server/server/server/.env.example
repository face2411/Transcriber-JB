PORT=4000
# Absolute or relative (to server/) path for the JSON data store.
# Defaults to server/data if unset.
DATA_DIR=./data
# Used by the /api/anthropic/* routes added in a later step.
ANTHROPIC_API_KEY=
# Stubbed for future integration - see spec.md "Future Integrations".
HUBSPOT_API_KEY=
APOLLO_API_KEY=
HUNTER_API_KEY=
